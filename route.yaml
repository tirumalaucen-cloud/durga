kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: nginx-service
  namespace: durga
  labels:
    app: nginx
spec:
  host: nginx-service-durga.apps.ocpv.lab.kuberox.net
  to:
    kind: Service
    name: nginx-service
    weight: 100
  port:
    targetPort: 80
  wildcardPolicy: None
